function bmiCalc(){const h=parseFloat(document.getElementById('height')?.value)/100,w=parseFloat(document.getElementById('weight')?.value),out=document.getElementById('bmiResult'); if(!h||!w||!out)return; const bmi=(w/(h*h)).toFixed(1); let cat=bmi<18.5?'Underweight':bmi<25?'Normal':bmi<30?'Overweight':'Obese'; out.innerHTML=`BMI: <b>${bmi}</b> - ${cat}`;}
function bpCalc(){const s=parseInt(document.getElementById('systolic')?.value),d=parseInt(document.getElementById('diastolic')?.value),out=document.getElementById('bpResult'); if(!s||!d||!out)return; let cat='Normal'; if(s<120&&d<80)cat='Normal'; else if(s<130&&d<80)cat='Elevated'; else if(s<140||d<90)cat='High BP Stage 1'; else cat='High BP Stage 2'; out.innerHTML=`Blood Pressure Status: <b>${cat}</b>`;}
function dragStart(e){e.dataTransfer.setData('text/plain',e.target.id)}function allowDrop(e){e.preventDefault()}function dropMeal(e){e.preventDefault();const id=e.dataTransfer.getData('text/plain');const item=document.getElementById(id); if(item)e.currentTarget.appendChild(item.cloneNode(true));}
function startTimer(seconds,id){let left=seconds;const el=document.getElementById(id);const t=setInterval(()=>{const m=Math.floor(left/60),s=left%60; el.textContent=`${m}:${String(s).padStart(2,'0')}`; if(left--<=0)clearInterval(t);},1000)}
